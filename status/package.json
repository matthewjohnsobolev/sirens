{
  "name": "sirens-status",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler pages dev public",
    "dev:remote": "wrangler pages dev public --remote",
    "deploy": "wrangler pages deploy public --project-name sirens-status",
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20240512.0",
    "typescript": "^5.4.5",
    "vitest": "^2.1.9",
    "wrangler": "^3.56.0"
  }
}
