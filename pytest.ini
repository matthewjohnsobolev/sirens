[pytest]
asyncio_mode = auto
addopts =
    -v
    --cov=alerts
    --cov=web
    --cov=config
    --cov-report=term-missing
    --cov-fail-under=95
pythonpath = .
testpaths = tests
filterwarnings =
    error::RuntimeWarning
