[build-system]
requires = ["setuptools>=84.0.0"]
build-backend = "setuptools.build_meta"

[project]
name = "Sirens"
version = "1.1.0"
requires-python = ">=3.10"
dynamic = ["dependencies"]

[project.scripts]
sirens-ops = "ops.cli:cli"

[tool.setuptools.dynamic]
dependencies = { file = ["requirements.txt"] }

[tool.setuptools]
packages = ["alerts", "bi", "data", "domain", "ops", "web"]
py-modules = ["config"]

[tool.setuptools.package-data]
alerts = ["img/*.png"]

[tool.coverage.report]
exclude_lines = [
    "pragma: no cover",
    "if __name__ == .__main__.:",
]

[tool.ruff]
line-length = 100
target-version = "py310"

[tool.ruff.lint]
select = ["E", "F", "W", "I", "UP", "B"]
ignore = ["E501"]